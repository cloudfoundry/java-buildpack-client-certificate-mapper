name: CI

on:
  push:
    paths-ignore:
      - '.github/**'
      - 'README.md'
      - 'LICENSE'
      - 'NOTICE'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '.github/**'
      - 'README.md'
      - 'LICENSE'
      - 'NOTICE'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Set up Java 21
        uses: actions/setup-java@v6.0.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and test
        run: ./mvnw -q package

      - name: Upload jar artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: java-buildpack-client-certificate-mapper
          path: |
            java-buildpack-client-certificate-mapper/target/*.jar
            !java-buildpack-client-certificate-mapper/target/*-sources.jar
            !java-buildpack-client-certificate-mapper/target/*-javadoc.jar
            !java-buildpack-client-certificate-mapper/target/original-*.jar
          if-no-files-found: error

  snapshot:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Set up Java 21
        uses: actions/setup-java@v6.0.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build
        run: ./mvnw -q package -DskipTests

      - name: Collect artifacts
        id: artifacts
        run: |
          JAR=$(ls java-buildpack-client-certificate-mapper/target/java-buildpack-client-certificate-mapper-*SNAPSHOT.jar \
                | grep -v 'sources\|javadoc\|original' | head -1)
          SOURCES=$(ls java-buildpack-client-certificate-mapper/target/java-buildpack-client-certificate-mapper-*SNAPSHOT-sources.jar \
                    | head -1)
          echo "jar=$JAR" >> $GITHUB_OUTPUT
          echo "sources=$SOURCES" >> $GITHUB_OUTPUT

      - name: Recreate rolling snapshot release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          gh release delete snapshot --yes 2>/dev/null || true
          git push origin :refs/tags/snapshot 2>/dev/null || true
          gh release create snapshot \
            --prerelease \
            --title "Latest SNAPSHOT ($VERSION)" \
            --notes "Automatically published on every push to \`main\`. Always reflects the latest snapshot build." \
            "${{ steps.artifacts.outputs.jar }}" \
            "${{ steps.artifacts.outputs.sources }}"
